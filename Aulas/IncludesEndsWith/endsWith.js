// o comando endsWith serve para verificar se a variavel termina com o parametro passado
// da mesma forma podemos usar o startWith
let nomes = "Alex";
console.log(nomes.endsWith('lex'))

console.log(nomes.startsWith('Al'))